
export function PricesTitle({title}: {title: string}) {
  return (
    <span className="text-3xl font-bold mb-2 text-center text-[var(--primary-text-color)] tracking-tight">
      {title}
    </span>
  )
}